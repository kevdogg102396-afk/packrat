# Edge Case Test File

## Dollar Signs in Code
Here's some bash: `export PRICE=$49.99` and `echo $HOME/.config`.
JavaScript: `const total = $amount + $tax;`
Template literal: `${user.name} has ${count} items`

## Regex Metacharacters
File path: C:\Users\testuser\projects\packrat\index.mjs
Glob pattern: **/*.{ts,tsx}
URL with params: https://api.example.com?q=hello&limit=10
Math: (2+3) * [4-1] = 15
Pipe: cat file.txt | grep "pattern" | head -n 5

## Unicode
Emoji test: This is great! The project is awesome
CJK: some testing text here
Accented: cafe, naive, resume, uber

## Empty Sections


## Markdown Tables
| Tool | Status | Notes |
|------|--------|-------|
| PackRat | Active | Compression tool |
| DJclaude | Active | Music gen |

## Code Blocks
```javascript
function compress($text, codebook) {
  const $result = codebook.allPairs();
  for (const { code, text: $pattern } of $result) {
    $text = $text.replace(new RegExp($pattern, 'gi'), code);
  }
  return $text;
}
```

## Nested Formatting
**Bold with `code` inside** and *italic with [links](https://example.com)*

## Very Long Line
This is a very long line that goes on and on and on and contains many words and entities like TypeScript and React and Supabase and Vercel and GitHub and Telegram and Docker and Cloudflare and all sorts of technical terms like webpack and vite and tailwind and supabase and vercel and cloudflare and docker and kubernetes and terraform and ansible.
